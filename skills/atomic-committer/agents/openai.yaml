interface:
  display_name: "Atomic Committer"
  short_description: "Group dirty git changes into guarded atomic commits"
  default_prompt: "Use $atomic-committer to inspect the dirty git tree, block live-looking credential assignments and private-key material across common providers, group safe files into logical changesets, commit with English conventional prefixes and Korean messages, and push only when a remote exists and push was requested."
