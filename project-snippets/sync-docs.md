## Project Skills

- Use $sync-docs at <skills-root>/skills/sync-docs/SKILL.md when asked to review, refresh, reconcile, or update documentation by comparing existing docs, snippets, history, skills, root or folder-local AGENTS.md indexes, target project skill setup, and repo instruction files.

## Project-Specific Overrides

- Treat the current repo files as the first evidence source.
- When folder structure changes, refresh the nearest folder-local AGENTS.md table-of-contents index and its local map, do-not-change boundaries, related skills, and validation commands.
- When validating project skill setup, compare the target instruction file with selected snippets, actual `skills/*/SKILL.md` paths, neighboring `skill.html` files, project-specific overrides, and the no-global-install rule.
- Ask before changing a documentation rule when the source of truth is unclear.
- Keep history ledgers durable; do not turn them into scratch review notes.
- Run available documentation, repo, and skill validators after edits.
